services:
  - type: web
    name: sales-kiosk
    env: node
    plan: starter
    buildCommand: npm run build
    startCommand: npm run start
    envVars:
      - key: MONGO_URI
        sync: false
      - key: PORT
        value: 5000
